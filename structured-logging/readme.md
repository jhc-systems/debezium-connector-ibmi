# overview
output structured json messages

# notes
log4j2 when included as a dependency will automatically create a log4j2.dat file that includes info for the plugin
this dependency then needs to come above the other log4j dependencies in the consuming project